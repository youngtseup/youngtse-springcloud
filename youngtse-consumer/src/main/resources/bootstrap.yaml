spring:
  application:
    name: my-consumer
server:
  servlet:
    context-path: /consumer
    # 禁止创建cookie
    session:
      cookie:
        name: ""
        http-only: true
        path: /consumer
        secure: true
